{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build and run C++",
      "type": "shell",
      "command": "bash",
      "args": [
        "-c",
        "clang++ -std=c++17 -Wall -o ${workspaceFolder}/temp_program ${file} && ${workspaceFolder}/temp_program && rm ${workspaceFolder}/temp_program"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$gcc"]
    }
  ]
}
